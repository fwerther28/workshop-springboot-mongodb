services:
  mongodb:
    image: mongo:latest
    container_name: workshop-mongo
    ports:
      - "27017:27017"
    environment:
      - MONGO_INITDB_DATABASE=workshop_mongodb
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: